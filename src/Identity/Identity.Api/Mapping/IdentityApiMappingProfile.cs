using DigiTrade.Common.Mapping;

namespace Identity.Api.Mapping;

public sealed class IdentityApiMappingProfile()
    : AssemblyScanningMappingProfile(typeof(IdentityApiMappingProfile).Assembly);